# ==============================================================================
# Use Case: Docker Compose - LOADTEST Environment
# Description: Multi-container setup for loadtest testing.
# 
# Explanation: This file is part of the Docker suite. It provides a standardized template for docker-compose-loadtest.yml to maintain infrastructure and application reliability.
# 
# How to Use:
# Run 'docker-compose -f docker-compose-loadtest.yml up -d' to start the environment in the background.
# ==============================================================================
version: '3.8'
services:
  app:
    image: myapp:loadtest
    ports:
      - "8080:8080"
  db:
    image: postgres:13
    environment:
      POSTGRES_DB: loadtest_db
