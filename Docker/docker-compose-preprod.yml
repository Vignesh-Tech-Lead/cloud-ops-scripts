# ==============================================================================
# Use Case: Docker Compose - PREPROD Environment
# Description: Multi-container setup for preprod testing.
# 
# Explanation: This file is part of the Docker suite. It provides a standardized template for docker-compose-preprod.yml to maintain infrastructure and application reliability.
# 
# How to Use:
# Run 'docker-compose -f docker-compose-preprod.yml up -d' to start the environment in the background.
# ==============================================================================
version: '3.8'
services:
  app:
    image: myapp:preprod
    ports:
      - "8080:8080"
  db:
    image: postgres:13
    environment:
      POSTGRES_DB: preprod_db
