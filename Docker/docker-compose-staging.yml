# ==============================================================================
# Use Case: Docker Compose - STAGING Environment
# Description: Multi-container setup for staging testing.
# 
# Explanation: This file is part of the Docker suite. It provides a standardized template for docker-compose-staging.yml to maintain infrastructure and application reliability.
# 
# How to Use:
# Run 'docker-compose -f docker-compose-staging.yml up -d' to start the environment in the background.
# ==============================================================================
version: '3.8'
services:
  app:
    image: myapp:staging
    ports:
      - "8080:8080"
  db:
    image: postgres:13
    environment:
      POSTGRES_DB: staging_db
